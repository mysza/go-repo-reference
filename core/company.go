package core

// Company is an organisation that has departments and employees.
type Company struct {
	ID   string
	Name string
}

// type OrganisationRepository interface {
// 	Store(organisation Organisation) error
// 	FindByID(id string) (Organisation, error)
// 	FindByName(name string) ([]Organisation, error)
// }
